[Unit]
Description=Start scx_simple
ConditionPathExists=/sys/kernel/debug/sched/ext

[Service]
Type=simple
ExecStart=/bin/sh -c '/usr/bin/scx_simple 2>&1 | grep -E "warn|error"'
Restart=always

[Install]
WantedBy=multi-user.target
